---
"@treramey/drag": patch
---

Add the safe capture-only `drag-companion` workspace binary and contract, including append-only evidence journaling, idempotent SQLite import, versioned persistence schemas, safe failure handling, and deterministic minimized daily evidence bundles.
