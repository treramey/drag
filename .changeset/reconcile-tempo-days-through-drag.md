---
"@treramey/drag": patch
---

Reconcile complete Tempo days in `drag-companion` through the Drag public CLI boundary with structured continuation reads, normalized audit comparisons, and dry-run payload previews that never perform live mutations.
