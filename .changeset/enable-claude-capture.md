---
"@treramey/drag": minor
---

Add explicit Claude Code activity capture consent to `drag setup`, including unattended dry-run planning, idempotent lifecycle hook installation, and local minimized event capture through the primary `drag` executable.
